platform: linux

image_resource:
  type: docker-image
  source: {repository: java, tag: 8}

inputs:
  - name: sb-sample-service

outputs:
  - name: build-output

run:
  path: "sb-sample-service/concourse/tasks/build.sh"